---
openapi: 3.0.0
info:
  title: Candidate
  description: This is the swagger of Candidate ressources
  version: 1.0.0
servers:
  - url: http:localhost:8082
    description: SwaggerHub API Auto Mocking

tags:
  - name: Candidate
    description: Manage any resources
paths:
  /create:
    post:
      tags:
        - Candidate
      summary: Create Candidate
      operationId: createCandidate
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Candidate'
      responses:
        '200':
          description: OK
          content:
            'application/json':
              schema:
                $ref: '#/components/schemas/CandidateFullData'
components:
  schemas:
    Candidate:
      type: object
      properties:
        firstName:
          type: string
          description: First name of the Candidate
        lastName:
          type: string
          description: Last name of the Candidate
        email:
          type: string
          description: email of the Candidate
    CandidateFullData:
      allOf:
        - $ref: '#/components/schemas/Candidate'
        - type: object
          properties:
            customerId:
              type: integer
