server:
  port: 8081
spring:
  application:
    name: employer-service
# PostgreSQL Data Source Configuration
  datasource:
        url: jdbc:postgresql://localhost:5432/employerDataBase
        username: myuser
        password: mypassword
  jpa:
        hibernate:
          ddl-auto: create
  sql:
        init:
          continue-on-error: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
